"""Model-parameter-estimation domain validation.

``backend.py`` holds the model families and estimation infrastructure;
``parameter_estimation.ipynb`` is the rerunnable report that imports it and
writes figures to ``figures/``.
"""
