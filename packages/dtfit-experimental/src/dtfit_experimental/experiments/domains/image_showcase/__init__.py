"""The image showcase: the image core's claims measured on two public
datasets larger than RAM (NGL GPS series, NOAA Global Hourly), on this PC
and on a Raspberry Pi.

See ``image_showcase.ipynb`` (the report) over the modules here (the
compute); ``backend.py`` re-exports the names the notebook uses.
"""
